id: secrets-in-files

info:
  name: Secrets in files
  author: ayadi momhamed
  severity: high
  tags: keys,file,secret

file:
  - extensions:
      - all
    extractors:
      - type: regex
        part: body
        regex:
          - "(password|pwd|passwd|pass|key|secret|private)(\\s{0,})(=|:|is|key)"
          - "(key|_key)(\\s{0,})(=|:|is)"
          - "(api|api_key|api-key|apikey)(\\s{0,})(=|:|is)"
